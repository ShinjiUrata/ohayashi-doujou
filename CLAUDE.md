# お囃子道場 (iOS)

このドキュメントは、本プロジェクトの**設計合意・技術選定・未決事項**をまとめたハンドオフ資料です。新しいClaude Codeセッションを開始したら、まずこのファイルを読んでからユーザーと対話を始めてください。

---

## 1. プロジェクトの目的

自治体の**秋祭り**で、子供たちが**お囃子のリズムを直感的に学べる**よう、太鼓の達人風のリズムゲームをiOSアプリとして開発する。

**コアユーザー**: 祭りに来場する子供たち
**コア体験**: 「お囃子を体で覚える」きっかけ作り。本物の太鼓に向かう前の導入。
**利用シーン**: 祭り会場のブースに置いたiPhoneを順番にタップして遊ぶ

### ビジネスモデル

- アプリは **App Store で一般公開**(無料インストール)
- **譜面のダウンロード(ID指定)とプレイは誰でも無料**
- **譜面の作成・編集・制作者自身による試作プレイは無料**
- **譜面を他者に配布するための唯一の手段 = 「公開(サーバーアップロード)」のみ課金**
   - ¥1,000 / 曲(Consumable、公開ボタン押下時の即時消費)
   - 資金決済法上の前払式支払手段に該当しない設計(購入と役務提供が同時)
   - 主な購入者は弊社の取引先である**近隣自治体**(自地域への配布のために購入)
- **譜面の配布手段は「ID入力DL」に一本化**(MVP)
   - プレイヤーは譜面検索画面でIDを入力 → サーバーから個別DL → ローカル保存
   - 制作者は公開時に得たIDを地域内で告知(口頭・ポスター・QR等)
   - 当面、AirDrop / Files / iCloud 等の端末間ファイル共有はアプリとして提供しない(設計のシンプルさ優先)
   - **将来の拡張**: AirDrop による譜面ファイル共有を後のアップデートで追加予定(運用上の利便性向上のため)
- **アカウントの概念なし**(認証なし)
- **スコアの共有なし**

### 通信

プレイ・録音は完全ローカル動作。ネット越しの通信は以下のみ:
- **個別譜面JSON の取得**(ID指定での匿名GET、`{id}.json`)
- 譜面公開時のアップロード(IAPトランザクション検証付き)

**譜面の一覧APIは設けない**。譜面の発見手段は ID 入力のみ。
配布モデルが「自治体内でIDを告知して使ってもらう」という形であり、譜面一覧の検索性を提供する必然性が薄いため。
(IDを推測されて他人が同じ譜面をDLしても、課金は完了済みなので問題ない)

### オフライン動作前提

本アプリは **譜面DL後はネットワーク不要で完結する設計** を採る:
- 譜面JSON は事前にDLしてアプリ専用ディレクトリに保存
- 効果音(ドン/カッ)はアプリバンドル同梱
- 描画・判定・スコア計算はすべてローカル

**理由**:
- 祭り会場は電波が極端に混雑することが多く、Wi-Fi も期待できない
- ブースに置いた1台のiPhoneを子供が連続で叩く運用 → プレイ中の通信失敗は致命的
- ネット接続が必要なのは「譜面DL時」と「公開時(制作者のみ)」だけに限定

---

## 2. プラットフォーム・開発環境

| 項目 | 内容 |
|---|---|
| プラットフォーム | iOS (iPhone, 縦持ち) |
| 言語 | Swift |
| ゲーム描画 | SpriteKit (SwiftUIでラップする想定) |
| 音声 | AVAudioEngine |
| 触覚 | UIImpactFeedbackGenerator |
| 開発機 | macOS + Xcode 26.0.1(確認済) |
| Swift | 6.2(確認済) |
| シミュレータ | iOS 18.6 / 26.0 利用可(iPhone 17 Pro等)|
| Apple Developer Program | ユーザー契約予定(別アプリでも使用、問題なし) |
| 配布形態 | TestFlight or Ad Hoc、当日は会場の1台で運用想定 |
| Bundle Identifier(本番) | `com.zembrem.ohayashidoujou` |
| Bundle Identifier(開発) | `com.zembrem.ohayashidoujou.dev` |
| 表示名(日本語) | お囃子道場 |

### なぜSpriteKit?
- リズムゲームの「ノーツ落下 + 当たり判定」が標準APIで素直に書ける
- `CADisplayLink`ベースで60/120Hzの安定したフレーム同期
- AVAudioEngine連携で低遅延オーディオ
- SwiftUI単体だと描画ループの扱いが面倒

### 不採用とした選択肢
- **Windows + タタコン**: 初期検討案。タタコン入力の不確実性、配布形態、ユーザー側設備の制約から不採用。子供向けにはiOS+タッチの方が運用が楽
- **Unity / Godot**: オーバースペック。Apple Developer登録もありネイティブで完結する方が良い
- **Web (PWA等)**: 触覚フィードバック・低遅延オーディオの面でネイティブに劣る

---

## 3. ゲーム仕様

### 3.1 モード構成

2モードのみ:

1. **録音モード** — お囃子の指導者(または上手な子)がお手本を叩き、譜面化する
2. **プレイモード** — 録音された譜面を選択して練習する

### 3.2 画面の向き
**縦持ち固定**。横持ち対応は不要。

### 3.3 プレイ画面のレイアウト

```
┌──────────────────────┐
│  ♪ お囃子の練習       │ ← ヘッダ(曲名 / スコア / コンボ)
│                      │
│  │  │   │  │         │ ← 4レーン(上→下)
│  ●                   │
│      ●               │   ノーツが落下
│         ●            │
│            ●         │
│  ◯  ◯   ◯  ◯         │ ← 判定目印(固定リング)
│  ━━━━━━━━━━━━━━━━━   │ ← 判定ライン(Hit Line)
│  ╭──────────╮        │
│  │   太鼓    │        │ ← 太鼓(上部 6〜7割を表示)
│  ╰──────────╯        │
└──────────────────────┘

レーン(視覚教育用、4本):
  1: 左カッ  → 太鼓の左外側 へ落下
  2: 左ドン  → 太鼓中央(左寄り) へ落下
  3: 右ドン  → 太鼓中央(右寄り) へ落下
  4: 右カッ  → 太鼓の右外側 へ落下

タップ判定ゾーン(物理的、3区分):
  - 太鼓の左外側  → ka_l (左カッ)
  - 太鼓の内側    → don (左ドン/右ドンの両方が正解)
  - 太鼓の右外側  → ka_r (右カッ)
```

### 3.4 ノーツ表現

| 種別 | 視覚 | 落下レーン | 判定ゾーン |
|---|---|---|---|
| `don_l`(左ドン) | 赤い大きめの円 + 「左」文字 | レーン2 | 中央(共通) |
| `don_r`(右ドン) | 赤い大きめの円 + 「右」文字 | レーン3 | 中央(共通) |
| `ka_l`(左カッ) | 青い小さめの円 + 「左」文字 | レーン1 | 左外側 |
| `ka_r`(右カッ) | 青い小さめの円 + 「右」文字 | レーン4 | 右外側 |

**重要**: ドン系は「叩く位置が物理的に同じ」(太鼓中央)なので、`don_l` と `don_r` のタップ判定は同一。レーンと文字は**叩く手の学習用ガイド**であり、判定には影響しない。お囃子では「どのタイミングをどちらの手で叩くか」が決まっているため、視覚的に手を示すことが教育目的上重要。

### 3.5 タイミング判定

太鼓の達人方式:
- ノーツが判定ラインと重なった瞬間にタップ
- 判定窓は**子供向けに緩めに設定**(暫定: ±200ms=「良」、±400ms=「可」、それ以上で「不可」)
- 数値は実装後に試遊して調整

### 3.6 録音モード(案2を採用)

通常時の画面とは異なり、**録音中だけ太鼓の面を左右に分割**する:

```
┌──────────────────────┐
│  [● REC] 00:12       │
│                      │
│                      │
│  ╭──────┬───────╮    │
│  │ 左面  │ 右面  │    │ ← 太鼓を左右に分割
│  │       │      │    │
│  ╰──────┴───────╯    │
│ ↑左カッ ↑左ドン │右ドン ↑右カッ
└──────────────────────┘
```

タップ位置 → 種別の対応:
- 太鼓の左外側 → `ka_l`
- 太鼓内側の**左半分** → `don_l`
- 太鼓内側の**右半分** → `don_r`
- 太鼓の右外側 → `ka_r`

→ 録音時はドンの左右をタップ位置で**推定**して記録する。完璧な精度は不要(指導者の意図が反映されれば十分)。録音後に編集画面で微修正できる構造を残しておくと尚良い。

### 3.7 譜面データ仕様

JSON形式、端末ローカル保存(Documentsディレクトリ)+ ネット越し配信:

```json
{
  "id": "shimoda-2026-irihayashi",
  "name": "獅子舞 入り囃子",
  "region": "下田町",
  "created_at": "2026-06-23T10:30:00Z",
  "duration_ms": 24500,
  "notes": [
    { "t": 0,    "type": "don_l" },
    { "t": 450,  "type": "ka_r"  },
    { "t": 900,  "type": "don_r" },
    { "t": 1350, "type": "ka_l"  }
  ]
}
```

- `id`: **完全ユーザー指定**
  - 制作者が公開時に任意の文字列を入力(例: `shimoda-2026-irihayashi`)
  - サーバー側は **早い者勝ち**で受付け、衝突したらエラー返却 → 別のIDで再試行
  - 文字種は小英字・数字・ハイフンに限定(URLパスとして安全な範囲)
  - 長さは 3〜64文字程度を想定
  - 推測されてもデメリットなし(公開済み譜面が他者にDLされることに支障はない)
- `t`: 録音開始からの経過ミリ秒
- `type`: `don_l` / `don_r` / `ka_l` / `ka_r` のいずれか
- プレイ時の判定では `type` を3グループに正規化:
  - `ka_l` → 左外ゾーン
  - `don_l` / `don_r` → 中央ゾーン
  - `ka_r` → 右外ゾーン

#### 譜面ファイルの保存と外部アクセス
譜面JSONはアプリ専用ディレクトリに保存し、MVPでは **アプリ内からのみ参照可能** とする(配布チャネルは ID入力DL に一本化されており、外部アクセスを開ける必要がないため):
- アプリ独自ディレクトリ配下に保存(Documents 直下ではなく、サブディレクトリで管理)
- `UIFileSharingEnabled = false`(Files アプリから不可視)
- `LSSupportsOpeningDocumentsInPlace = false`
- Document Picker / Share Sheet には譜面ファイルを渡さない
- 譜面の「共有」は **譜面IDの文字列共有のみ**(QRコード / コピー&貼付け等)

**将来の拡張(AirDrop 対応)**:
利便性向上のため、後のアップデートで `UIActivityViewController` 経由の AirDrop 共有機能を追加予定。
追加時は譜面JSONをそのまま共有可能にする(公開済み・未公開の区別は設けない単純な設計を想定)。

---

## 3.8 譜面共有(バックエンド) / IAP

### バックエンド構成(GCP)

| 用途 | サービス | 役割 |
|---|---|---|
| 譜面ファイル | Cloud Storage(公開バケット) | `{id}.json` を匿名GETで配信(一覧APIは設けない) |
| 公開API | Cloud Run | `/publish`(IAP検証 → ID重複チェック → GCS書き込み)と `/check-id`(IDの利用可否事前確認) |
| トランザクション台帳 | Firestore | 使用済み IAP トランザクションID、公開済み譜面ID の記録 |
| CDN | 当面なし | 規模が出たら Cloud CDN を前段に追加 |

#### アプリ視点のエンドポイント
- `GET https://charts.example.com/{id}.json` — 個別譜面DL
- `POST https://api.example.com/publish` — 公開(IAP決済成功後の即時アップロード、ID指定込み)
- `POST https://api.example.com/check-id` — 公開ボタン押下前のID重複事前確認(課金後に重複エラーで決済損失を防ぐため)

**譜面一覧APIは存在しない**。バケットの ListObjects 権限も付与しない。
譜面の発見手段は「制作者から ID を直接教えてもらう」のみ(配布モデルがそれで完結するため、検索一覧を実装する必然性が薄い)。

### 課金(StoreKit 2 / In-App Purchase)

**設計の根本方針**: **譜面の作成は無料、公開のみ課金(即時消費)**。
これにより資金決済法上の「前払式支払手段」に該当せず、有効期限管理・供託義務などの規制対象から完全に外れる(購入と役務提供が同時の「即時の役務提供」となるため)。

| 項目 | 内容 |
|---|---|
| プロダクトID | `rhythm.chart.publish.single`(仮) |
| タイプ | **Consumable**(1曲分の公開役務) |
| 課金タイミング | **公開ボタンを押した瞬間に課金 → 成功したら即 publish** |
| 価格 | **¥1,000 / 曲**(Apple 手数料 15-30% 後の手取り ¥700〜¥850) |
| 最小iOS | iOS 15+(StoreKit 2 要件) |
| DEBUG ビルド | バックエンドの URL を開発環境に振り向ける程度で十分。サンドボックスIAPで本番フロー検証可 |

#### 無料でできること(課金不要)
- 譜面の録音
- 譜面の編集
- 自端末ローカルでのプレイ・試遊
- 他者の公開譜面を ID入力 でDLしてプレイ

#### 課金が必要なこと
- 譜面の **公開**(サーバーへのアップロード)のみ
- 公開して得た IDを地域内で告知することで、はじめて他端末がDL可能になる

#### 公開フロー
```
[制作モード] 録音 → 編集 → 譜面名・地域名・公開ID 入力
   ↓ (ここまで全部無料、ローカル試遊もOK)
[公開ボタン押下]
   ↓
   POST /check-id { id } で重複事前確認
   ├ 使用済み → エラー表示「このIDは既に使われています」(課金前にここで止める)
   └ 利用可能
        ↓
[IAP決済ダイアログ] ¥1,000
   ├ キャンセル → 何も起きない(ローカル譜面は残る、再試行可)
   └ 購入成功
        ↓
        POST /publish { signed_transaction (JWS), chart_json }
        ↓
   [Cloud Run]
   ├ Apple App Store Server API で JWS を検証
   ├ Firestore で transaction_id 重複チェック(未使用か)
   ├ Firestore で chart_id 重複チェック(競合発生時はエラー、サポート対応)
   ├ GCS に {id}.json 保存
   ├ Firestore に { transaction_id, chart_id, created_at, region } 記録
   └ レスポンス: { id: "shimoda-2026-irihayashi" }
        ↓
   公開IDを表示(コピー / QR表示で地域内告知用に使う)
```

#### 譜面ダウンロードフロー(プレイ側)
```
[譜面検索画面] ID入力欄("shimoda-2026-a7Kp")
   ├ 手入力
   └ QRスキャン(将来対応)
   ↓
[ダウンロードボタン]
   ↓
   GET https://charts.example.com/{id}.json
   ↓
   ├ 200: 端末ローカル(アプリ専用ディレクトリ)に保存 → 一覧に追加
   └ 404: 「この譜面IDは見つかりません」表示

[保存済み譜面一覧] → 選択 → [プレイ画面] → オフラインプレイ可
```

#### 更新公開について
**実装しない**。修正したい場合は別IDで新規公開 + 旧版を取り下げ。
理由: 「更新」を許すと「取り下げ→再公開で課金回避」の抜け道になるため、設計を単純化。

### 譜面公開の運用
- 公開ID(`abc123`)は制作者に表示し、問題発生時の取り下げ依頼に利用
- 取り下げ機能の実装は Phase 6 以降で検討(現状は公開のみ)

---

## 4. 楽曲・BGMの扱い

**現段階では楽曲(BGM)は不要**。ノーツのみが流れる。

この決定により得られたメリット:
- オーディオ同期の難題が消滅(楽曲との位相合わせが不要)
- 残るレイテンシ要因は「入力遅延」「表示遅延」のみ → iOSタッチは高速なのでキャリブレーション機能も省略可能

ただし、**叩いた瞬間の効果音は必須**(ドン / カッの2種類)。打感の代替としても、子供の体験品質としても重要。

将来的に笛・鉦などの伴奏トラックを追加する余地は設計上残しておく。

---

## 5. UX上のフィードバック設計

子供向けに賑やかさを重視:
- **音**: ドン / カッの効果音(短いWAV、フリー素材で十分)
- **触覚**: `UIImpactFeedbackGenerator`で軽い振動
- **視覚**: タップ位置に波紋、太鼓が一瞬光る、判定文字(良/可/不可)を表示

---

## 6. 既存資産

### 6.1 プレイ画面のHTMLモック
`mockups/play.html`

- iPhoneフレーム風の縦持ち画面
- 4レーン + ノーツ落下アニメーション(CSS keyframes)
- 判定ライン + リング状の目印
- 太鼓(赤い和太鼓風グラデーション)
- 右側に凡例パネル
- **ユーザー確認済み、デザイン方向OK**

色味・速度・配置は実装時の参考に。これをそのままSwift側で再現するイメージ。

### 6.2 録音画面のモック
**未作成**。必要なら新セッションで作成可。

---

## 7. 実装ロードマップ(提案)

### Phase 1: 最小プロトタイプ(まずここを目指す)
- [ ] Xcodeプロジェクト作成(SwiftUI + SpriteKit)
- [ ] プレイ画面の静的レイアウト(太鼓・レーン・判定ライン・目印)
- [ ] ノーツ1個を上から下へ落下させる
- [ ] タップ判定(座標で3ゾーン判別)
- [ ] 効果音再生(ドン / カッ)
- [ ] シミュレータで動作確認

### Phase 2: 譜面再生
- [ ] 譜面JSONを読み込んで複数ノーツを時系列で生成
- [ ] 判定窓に基づく良/可/不可の判定
- [ ] スコア・コンボ表示
- [ ] リザルト画面

### Phase 3: 録音モード
- [ ] 太鼓を左右分割表示する録音画面
- [ ] タップを時系列で記録 → JSON出力
- [ ] 譜面の保存(`FileManager` + Documentsディレクトリ)
- [ ] 譜面一覧 / 選択画面

### Phase 4: バックエンド + 譜面共有
- [ ] GCP プロジェクト作成、Cloud Storage の公開バケット用意(ListObjects権限なし)
- [ ] Cloud Run の `/publish` 実装(IAP検証 → ID重複チェック → GCS書き込み)
- [ ] Cloud Run の `/check-id` 実装(ID事前確認)
- [ ] Firestore セットアップ(`transactions`, `charts` コレクション)
- [ ] iOS 側: 譜面検索画面(ID入力 → GET → ローカル保存)
- [ ] iOS 側: 保存済み譜面一覧
- [ ] iOS 側: 制作モードからの公開フロー(ID入力 → /check-id → IAP → /publish)

### Phase 5: IAP(StoreKit 2)
- [ ] App Store Connect で Consumable プロダクト登録(`rhythm.chart.publish.single`)
- [ ] iOS 側: 公開ボタン押下時の StoreKit 2 即時決済フロー
- [ ] Cloud Run 側: App Store Server API での JWS 検証
- [ ] サンドボックスでの「公開ボタン→決済→publish 成功」までの疎通テスト
- [ ] DEBUG 時はバックエンド URL を開発環境に振り向ける(IAP はサンドボックスでOK)

### Phase 6: 仕上げ
- [ ] 視覚エフェクト(波紋・光・判定文字)
- [ ] 触覚フィードバック
- [ ] タイトル画面 / メニュー
- [ ] 譜面の削除・リネーム機能(ローカルDL分の管理)
- [ ] アイコン・スプラッシュ
- [ ] 実機テスト(Apple Developer登録後)

### Phase 7: 祭り運用準備
- [ ] お囃子指導者によるお手本録音
- [ ] 譜面の動作確認・調整
- [ ] 判定窓の調整(子供向けに試遊して決める)
- [ ] 当日の運用テスト(複数の子供が連続でプレイするフロー)

---

## 8. 未決事項 / 新セッションでユーザーに確認すべきこと

1. **配色のトーン**: 現状モックは「祭りらしい朱色+黒+金」基調。子供向けにもっと明るくポップにする選択肢もあり
2. **ノーツの落下速度**: モックでは3秒。実機で試して調整
3. **判定窓の数値**: 暫定 ±200ms / ±400ms。要試遊
4. **連打ノート(連続ドン)** の対応有無
5. **両手同時打(大ドン)** の対応有無 — 現状仕様には含めていない
6. **アプリアイコン・スプラッシュ**のデザイン素材
7. **タイトル画面・メニュー画面**のデザイン方針
8. **譜面のサンプル**: 開発中の動作確認用に、簡単なテスト譜面を1〜2個用意したい
9. **GCP プロジェクト名**(本番 / 開発の分け方)、**配信ドメイン**(例: `charts.example.com` / `api.example.com`)
10. **IAP プロダクトID**(仮: `rhythm.chart.publish.single`)、価格ティア確定、サンドボックス用 Apple ID 用意
11. **譜面の取り下げフロー** の運用方針(管理ツールの形、ユーザーからの取り下げ依頼受付方法)
12. **利用規約の準備**(IAP 関連、課金は公開時即時消費・キャンセル不可、譜面公開のポリシー など)
13. **QRコード対応の Phase**(公開ID告知用 / プレイ側のID入力補助としての QR スキャン)
14. **AirDrop 共有機能の Phase**(将来アップデート対応として位置付け済み、優先度を別途決定)

---

## 9. 設計上の重要判断ログ(なぜそうしたか)

| 判断 | 理由 |
|---|---|
| iOSに変更(初期はWindows + タタコン) | 子供向け運用の容易さ、配布の容易さ、タタコン互換の不確実性回避 |
| 楽曲なし(ノーツのみ) | オーディオ同期問題を回避、シンプルさ重視 |
| 4レーン表示 / 3区分判定 | お囃子の「どの手で叩くか」を視覚的に教育しつつ、物理的に区別不能な「ドン中央」は判定を分けない現実的設計 |
| 録音モードはタップ位置で左右ドンを推定(案2) | 録音操作の単純さを優先。指導者が左右を意識して叩けば自然に記録される |
| 判定はタイミングのみ(縦の重なり) | 太鼓の達人ライクで子供にもわかりやすい |
| 縦持ち固定 | iPhoneの自然な持ち方、画面下=太鼓 という直感的レイアウト |
| キャリブレーション機能なし | iOSタッチの低遅延 + 楽曲なし構成のため不要と判断 |
| 譜面共有はネット越し(GCS公開バケット) | 自治体外への展開も視野。ローカル配布だけだとビジネスとして広がらない |
| バックエンドは GCP(Cloud Storage + Cloud Run + Firestore) | ユーザーが普段GCPを利用。譜面数が当面数十程度なので無料枠でほぼ収まる |
| 譜面制作は無料、公開のみ IAP(Consumable / ¥1,000 / 曲、即時消費) | (1) 資金決済法上の前払式支払手段に該当せず規制対象外、有効期限管理不要。(2) 試作・練習・私的利用は無料で済むため UX が良い。(3) 「公開」という具体的な役務に対する課金で心理的納得感が高い。(4) クレジット概念がない分、サーバー実装も最小 |
| 更新公開機能は持たない | 「取り下げ→再公開で課金回避」を防ぐため。修正したい場合は別IDで新規公開 |
| 譜面の発見手段はID入力のみ(一覧APIなし) | 配布モデルは「制作者がIDを地域内で告知 → プレイヤーがIDを入力してDL」で完結する。検索一覧APIを実装する必然性が薄いため、MVPでは含めない |
| 公開IDは完全ユーザー指定(早い者勝ち) | 制作者が地域名・曲名など覚えやすいIDを付けられる方が告知しやすい。公開済み譜面はIDを推測されてDLされても課金は完了済みのため問題なし |
| 譜面配布チャネルは MVP では ID入力DL に一本化 | 設計をシンプルに保つため。AirDrop 等の端末間ファイル共有はあとから追加できる利便性機能で、初期実装に含める必然性は薄い。将来のアップデートで追加予定 |
| 譜面はDL後オフラインで完結する設計 | 祭り会場の電波・Wi-Fi 事情が安定しないため、プレイ中の通信を要求する設計はリスクが高い。譜面JSONは極めて軽量(数十KB)で事前DLのコストも無視できる |
| アカウント / 認証なし | 仕様(スコア共有なし・公開譜面のみ)的に不要、UX を最大限シンプルに |
| Bundle ID prefix は `com.zembrem` に統一 | 既存の afterglow アプリ (`com.zembrem.spanglow`) と同一 Apple Developer アカウントで運用するため、prefix を揃えて証明書・プロビジョニングプロファイル管理を単純化する |
| Bundle ID 本体は `ohayashidoujou`(ハイフンなし) | (1) プロジェクト名「お囃子道場」と1:1対応、(2) Bundle ID のハイフンは技術的にはOKだが慣例的に避けられる、(3) 将来「お囃子」ブランドで別アプリを出す余地を残すため個別名詞にしている |

---

## 10. ユーザー(開発者)のコンテキスト

- macOS環境で開発(`/Users/urata/Devlopment/...` 配下で作業)
- 既にApple Developer Program利用予定(別アプリも開発)
- Swift / iOS開発の経験度合いは未確認 → 新セッションでコード解説の粒度を確認すべし
- 議論は日本語で進めてきた → 引き続き日本語で対応
- 仕様の磨き込みに丁寧で、決定の理由を重視する傾向(本ドキュメントもその姿勢で書かれている)
- auto mode で作業することがある(自律的に進めてOK、destructive な操作だけは確認)

---

## 11. 新しいClaudeセッションでの初動

このドキュメントを読み終わったら、以下の流れを推奨:

1. **挨拶 + 状況確認**: 「ハンドオフドキュメントを確認しました。プロジェクト名と最初のステップ(Phase 1のプロジェクト作成)から始めますか?」と聞く
2. **未決事項(セクション8)を必要に応じて確認**
3. **Phase 1から着手** — Xcodeプロジェクトを作る

最初の数ターンで聞くべきこと:
- プロジェクト名(英名 + 日本語表示名)
- Bundle identifier の prefix
- 最小iOSバージョン(iOS 17以上を推奨)
- ターゲット端末(iPhoneのみか、iPad対応もするか)

---

## 12. ファイル構成(現状)

```
ohayashi-doujou/
├── .gitignore
├── CLAUDE.md                       ← このファイル(設計合意ドキュメント)
├── infra-proposal.html             ← GCP インフラ構成提案書
├── dev_documents/
│   └── implementation_notes/
│       └── cost_protection.md      ← 必須実装項目(ログ量制御 / GCS大量DL対策)
├── mockups/
│   └── play.html                   ← プレイ画面のHTMLモック(動作確認用)
├── ios/                            ← Xcode プロジェクト(Phase 1 で作成予定)
└── backend/                        ← Cloud Run コード(Phase 4 で作成予定)
```

GitHub リポジトリ: <https://github.com/ShinjiUrata/ohayashi-doujou>(Private)
